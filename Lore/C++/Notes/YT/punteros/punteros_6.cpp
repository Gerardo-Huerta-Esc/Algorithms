/*FUNCIONES DEVOLVIENDO PUNTEROS

-Acceso a estructura de datos o clases: Devolciendo un puntero a una instancia, permites que otras partes
de tu programa interactúen con ella.

-Eficiencia en la gestión de la memoria: Al devolver un puntero, evitas la copia innecesaria de datos, 
ya que simplemente estás pasando la dirección de memoria donde reside el objeto o los datos

-Control sobre la vida útil de los objetos: Cuando devuelves unpuntero a un objeto creado dinamicamente 
(por ejemplo, usando new), puedes controlar cuanto tiempo existe ese objeto. El objeto puede seguir existiendo 
incluso después de que la función que lo creó haya terminado su ejecución.

- Funciones que devuelven múltiples valores: Devolver un puntero a una estructura o arreglopuede ser una forma
mas eficiente de devolver multiples valores, especialmente si el tamaño del resultado no se conoce de antemano.

-Interfaz de funciones polimórficas: Permite que la función devuelva objetos de cualquiera de las clases derivadas, 
manteniendo la flexibilidad y permitiendo al código que llama tratar esos objetos de manera uniforme a través de
punteros a la clase base.


Ahora se vera algo sobre punteros inteligentes:
*/

#include <iostream>
#include <string>
using namespace std;


int main(){

 
 
}


